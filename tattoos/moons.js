function setup() {
  createCanvas(400, 400);
  translate(100, 100)
  noFill()
  stroke(0)
  blendMode(HARD_LIGHT)
  ellipse(0, 0, 80, 80)
  // fill(255)
  noFill()
  noStroke()
  stroke(0)
  ellipse(20, 0, 80, 80)
}

function draw() {
  // background(220);

}